definitions:
  User:
    type: object
    required:
      - email
      - password
    properties:
      id:
        type: integer
        format: int64
      email:
        type: string
      username:
        type: string
      password:
        type: string
        format: password
